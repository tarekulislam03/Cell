[package]
name = "cell-terminal"
version = "0.1.0"
description = "CELL Terminal Emulator — Anime-themed native terminal"
authors = ["tarekul"]
edition = "2021"

[lib]
name = "cell_terminal_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
portable-pty = "0.8"
tauri-plugin-store = "2"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
